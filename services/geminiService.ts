// 此服務已被移除，因為專案決定回歸傳統光學演算法。
// This file is deprecated as per the jury's decision to remove AI dependencies.
// Keeping the file structure for potential future extensions if needed, but it exports nothing active.

export class GeminiService {
  constructor() {
    console.log("GeminiService has been disabled.");
  }
}

export const geminiService = new GeminiService();